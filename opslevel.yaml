service:
  import:
    - selector:
        kind: deployment
        namespace: infra
        labels:
          app: infra
      opslevel:
        name: \(.metadata.name)-\(.metadata.labels.component)
        description: .metadata.annotations."opslevel.com/description"
        aliases: 
        - .metadata.labels.alias
        - .metadata.annotations
        product: infra
        owner: 
        tier: 
        tags:
        - "{InternalProduct: .metadata.labels.IP}"
        - .metadata.labels
    - selector:
        kind: deployment
      opslevel:
        product: bookstore